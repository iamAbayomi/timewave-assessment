/* eslint-disable no-unused-vars */
import React from 'react';

interface props {
  xAxisData: any[];
  yAxisData: any[];
}

const WorkspaceGraphs = ({ xAxisData, yAxisData }: props) => {
  // const options: ApexOptions = {};

  return <div>{/* <ReactApexChart type="" options={} series={} height={} /> */}</div>;
};

export default WorkspaceGraphs;
